// Paste your Google Apps Script Web App URL here (ends in /exec).
// See ../apps-script/Code.gs for the backend and ../README.md for setup steps.
// This URL is safe to publish - it only accepts requests shaped like our own
// app's calls, and every action except login/createUser requires a valid
// session token.
export const BACKEND_URL = "REPLACE_ME";
